---
description: Delete a release — remove requirements, optionally revert code
argument-hint: "[name]"
---

Read `ai-instructions/commands/tmpl-release-delete.md` from the project root and
follow every step in order. Never delete the `init` release. Always show
impact and wait for the user's choice before removing anything.

Release name (may be empty): $ARGUMENTS
